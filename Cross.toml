[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture arm64",
    "apt-get update",
    "apt-get install -y libcfitsio-dev:arm64 pkg-config"
]

[build.env]
passthrough = [
    "PKG_CONFIG_PATH",
    "PKG_CONFIG_LIBDIR",
]
